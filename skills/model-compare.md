Compare the ML models in this project. For each model folder, extract and summarize:

1. **Architecture** — model type (XGBoost, sklearn, two-stage, ensemble, etc.)
2. **Features used** — input columns and any feature engineering
3. **Evaluation metrics** — accuracy, AUC, RMSE, F1, etc. from eval scripts or output files
4. **Key hyperparameters** — n_estimators, max_depth, learning_rate, etc.
5. **Notable differences** — what makes this model different from the others (e.g., different feature set, per-group splits, different architecture)

Present results as a comparison table. Flag which model appears strongest based on metrics, and note any models that seem experimental vs. production-ready.
